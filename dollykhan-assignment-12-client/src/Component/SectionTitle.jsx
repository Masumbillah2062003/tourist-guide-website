
const SectionTitle = ({headingTitle}) => {
    return (
        <div>
            <h1 className="pb-12 text-5xl text-center text-pink-600 font-bold">{headingTitle}</h1>
        </div>
    );
};

export default SectionTitle;